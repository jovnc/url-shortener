{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "fe": "pnpm --filter ./apps/frontend",
    "be": "pnpm --filter ./apps/backend",
    "dev": "docker compose up -d && concurrently --names backend,frontend --prefix-colors cyan,magenta \"pnpm be start:dev\" \"wait-on tcp:3001 && pnpm fe dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.0",
  "private": "true",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.5.3",
    "wait-on": "^9.0.5"
  }
}
