{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fe": "pnpm --filter ./apps/frontend",
    "be": "pnpm --filter ./apps/backend",
    "dev": "docker compose up -d && concurrently --names backend,frontend --prefix-colors cyan,magenta \"pnpm be start:dev\" \"pnpm fe dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "private": "true",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
